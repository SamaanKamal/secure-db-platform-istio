apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "service-mesh-database.fullname" . }}-test-connection"
  labels:
    {{- include "service-mesh-database.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "service-mesh-database.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
